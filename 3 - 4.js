let message = "Coding is great"
console.log(message)

let studentCount = 0
console.log(studentCount)

let messageChange = message.replace("Coding is great", "Coding might not be easy, but still great")
console.log(messageChange)

studentCount = 16
console.log(studentCount)

console.log(message.toLowerCase())

studentCount++
console.log(studentCount)